$("#<%= @share_uid %>-like").removeClass("pre-action").attr("href", '<%= facebook_shares_unlike_path(@share_uid) %>');

var likeCount = parseInt($("#<%= @share_uid %>-like_count").html());

$("#<%= @share_uid %>-like_count").html(likeCount += 1);
